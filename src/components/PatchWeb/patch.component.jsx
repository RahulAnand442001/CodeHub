import React from "react";
import "./patch.styles.css";

const PatchTemplate = () => {
	return (
		<div className="patch-container">
			<h1 className="patch-title">
				Sorry, currently the webpage is only supported in Desktop !
			</h1>
		</div>
	);
};

export default PatchTemplate;
